apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: smart-healthcare
  name: service-prescription
  labels:
    app: service-prescription
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-prescription
  template:
    metadata:
      labels:
        app: service-prescription
    spec:
      containers:
        - name: service-prescription
          image: service-prescription:v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8088
          envFrom:
            - configMapRef:
                name: service-prescription-config
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: database-secret
                  key: DB_URL
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: database-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: database-secret
                  key: DB_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8088
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8088
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
