spring.application.name=service-doctor
server.port=8082

# TODO: Database setup pending — placeholders for Supabase credentials.
# Replace [SUPABASE_HOST], [SUPABASE_USER], and [SUPABASE_PASSWORD] before connecting.
spring.datasource.url=jdbc:postgresql://[SUPABASE_HOST]:5432/postgres?currentSchema=patient_schema
spring.datasource.username=[SUPABASE_USER]
spring.datasource.password=[SUPABASE_PASSWORD]
spring.jpa.hibernate.ddl-auto=update
