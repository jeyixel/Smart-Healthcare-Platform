apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-appointment
  namespace: smart-healthcare
  labels:
    app: service-appointment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-appointment
  template:
    metadata:
      labels:
        app: service-appointment
    spec:
      containers:
        - name: service-appointment
          image: service-appointment:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: service-appointment-config
          env:
            - name: APPOINTMENT_DB_URL
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: APPOINTMENT_DB_URL
            - name: APPOINTMENT_DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: APPOINTMENT_DB_USERNAME
            - name: APPOINTMENT_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: APPOINTMENT_DB_PASSWORD
            - name: APPLICATION_SECURITY_JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: APPLICATION_SECURITY_JWT_SECRET_KEY
            - name: APPLICATION_SECURITY_JWT_EXPIRATION
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: APPLICATION_SECURITY_JWT_EXPIRATION
            - name: APPLICATION_SECURITY_JWT_REFRESH_EXPIRATION
              valueFrom:
                secretKeyRef:
                  name: smart-healthcare-secrets
                  key: APPLICATION_SECURITY_JWT_REFRESH_EXPIRATION
            - name: JAVA_TOOL_OPTIONS
              value: "-Xms256m -Xmx512m -XX:+UseG1GC -XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8083
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
