apiVersion: v1
kind: Service
metadata:
  namespace: smart-healthcare
  name: service-appointment
  labels:
    app: service-appointment
spec:
  selector:
    app: service-appointment
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP
